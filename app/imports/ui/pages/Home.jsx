import React from "react";
import { Meteor } from "meteor/meteor";
const Home = (props) => {
    return (
        <div>
            test
        </div>
    );
    
};

export default Home;
