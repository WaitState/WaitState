import React from "react";
import { Meteor } from "meteor/meteor";
const Register = (props) => {
    return (
        <div>
            Register Page
        </div>
    );
};

export default Register;